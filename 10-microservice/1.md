# Small tasks

## Написать контракты

Написать в папке api openapi.yaml спецификацию и gRPC proto.
Убедиться, что в REST и gRPC консистентные модели данных (одинаковый формат ID, timestamps, и т. п.)


## Скелет сервиса

В cmd/mysvc/main.go соединить config, logger, DB, repo, HTTP server, gRPC server


## Конфиг + healthcheck

Читать конфиг для подключения к БД из env переменных, использовать его.

Добавить эндпойнт /healthz, который отвечает 200, если сервис жив-здоров (надо реально пинговать БД), и 503, если не
в порядке.
