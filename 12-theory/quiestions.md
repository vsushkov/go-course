1. Опишите, как устроена обработка HTTP-запроса в net/http: что такое http.Server, Handler, ServeMux, и как запрос проходит через middleware до конкретного handler’а.
2. Как бы вы спроектировали единый формат ошибок для HTTP API (коды, сообщения, детали, request_id)? Какие поля вы бы включили и почему?
3. Расскажите, как вы реализуете ручную валидацию входящих JSON-запросов в Go без сторонних библиотек: где она должна жить, как тестируется, как возвращать ошибки по полям.
4. Объясните разницу между PUT и PATCH в REST API. Как вы бы реализовали PATCH так, чтобы отличать "поле отсутствует" от "стереть значение в этом поле"?
5. Как вы организуете маршрутизацию и извлечение path-параметров в Go 1.22+ с http.ServeMux? Какие есть ограничения и как вы их обходите?
6. Опишите подход к graceful shutdown для HTTP-сервера: какие сигналы обрабатываются, что делает Shutdown, какие таймауты важны и почему.
7. Что такое middleware в net/http и как вы строите цепочку middleware? Приведите пример порядка middleware (request_id, logging, auth, recovery) и объясните, почему порядок важен.
8. Объясните, что такое ETag/If-Match и как это помогает реализовать оптимистическую конкуренцию. Как бы вы реализовали это для ресурса в памяти и в базе данных?
9. Расскажите, как работает database/sql: что такое *sql.DB (пул), как он управляет соединениями, и почему его создают один раз на приложение.
10. Какие настройки пула соединений (SetMaxOpenConns, SetMaxIdleConns, SetConnMaxIdleTime, SetConnMaxLifetime) вы бы выбрали для микросервиса и как бы обосновали эти значения?
11. В каких ситуациях возникает sql.ErrNoRows? Как вы маппите его на доменные ошибки и на HTTP/gRPC статусы?
12. Опишите, как вы проектируете repository layer: зачем он нужен, где проходит граница между handler/service/repository, и как это помогает тестированию.
13. Как вы реализуете транзакции в Go (BeginTx, Commit, Rollback) и какие типичные ошибки допускают при работе с транзакциями? 4
14. Объясните, что значит "добавить audit/event запись в той же транзакции". Какие гарантии это даёт и какие проблемы предотвращает?
15. Опишите JWT (HS256) на практическом уровне: какие claims вы используете, как проверяете подпись и срок действия, и как организуете middleware для аутентификации.
16. Как вы проектируете авторизацию поверх JWT: роли, claims, ownership (владелец ресурса). Где лучше размещать проверки — в handler’ах или в сервисном слое — и почему?
17. Опишите "refresh-like" подход для JWT: как вы бы сделали короткоживущий access token и механизм перевыпуска. Какие компромиссы по безопасности возникают?
18. Как вы реализуете ротацию ключей для HS256 (current + previous secret)? Как это влияет на валидацию токенов и на процесс деплоя?
19. Расскажите о конкурентности в Go: чем отличаются goroutine, channel, mutex, RWMutex, atomic. Какие типичные race conditions встречаются в веб-бэкенде?
20. Как вы проектируете bounded queue и backpressure для фоновых задач (worker pool)? Как вы решаете, когда возвращать 503, и какие альтернативы возможны?
21. Опишите graceful shutdown для фоновых воркеров: как остановить приём новых задач, как "осушить" очередь, как ограничить ожидание таймаутом, и какие есть подводные камни.
22. Объясните роль context.Context в веб-сервисе: откуда он берётся, как распространяется, как влияет на отмену запросов при disconnect клиента.
23. Как вы реализуете таймауты на уровне запроса и на уровне отдельных операций (DB, внешние вызовы)? Как вы выбираете значения таймаутов?
24. Опишите паттерн "parallel subcalls" с общим контекстом: как запускать несколько операций параллельно, отменять остальные при первой ошибке и избегать утечек goroutine.
25. Что такое errgroup? Как бы вы реализовали минимальную версию на стандартной библиотеке и какие гарантии она должна давать?
26. Объясните основные концепции gRPC: что такое service, method types (unary/streaming), proto-контракт, и как вы организуете версионирование API (v1/v2).
27. Как устроены interceptors в gRPC и чем они похожи на HTTP middleware? Какие задачи вы бы вынесли в interceptors (логирование, ..., ...) и почему?
28. Как вы пишете тесты в Go для HTTP: чем отличаются unit и integration тесты, как использовать httptest?
29. Как вы пишете тесты в Go для gRPC, как тестировать gRPC через bufconn?
30. Опишите стратегию наблюдаемости: structured logging через log/slog (attrs, groups, levels), correlation/request_id (HTTP headers и gRPC metadata), метрики и базовые идеи трассировки (spans, propagation). Как вы бы связали логи, метрики и трейсы для диагностики инцидентов?
