# Small tasks

## Написать proto файл + сгенерировать код

Создай notes.proto и опиши в нем NotesService с унартными методами CreateNote, GetNote, ListNotes.
Сгенерируй код по proto файлу.


## gRPC клиент

Написать небольшой CLI клиент, который будет вызывать CreateNote, GetNote.
Запустить gRPC сервер, увидеть взаимодействие клиента и сервера.


## Реализовать сервер

Реализуй хендлеры CreateNote, GetNote, ListNotes, чтобы возвращались хорошие ответы, а не unimplemented method.


## Добавить интерцептор

Добавить интерцептор, который будет добавлять (если ещё не добавлен) request ID в контекст запроса
