# Big task

## Базовый минимум

Добавить интерцептор для JWT auth (получать токен из context metadata и валидировать его)


## Посложнее

Добавить streaming метод ListNodesStream (добавить time.Sleep, чтобы было в CLI клиенте видно, как по-немногу
приходит ответ от сервера)


## Роскошный максимум

Добавить дедлайны/таймауты при отправке запроса к gRPC серверу, убедиться, что все слои (вплоть до работы с БД)
быстро прекращают работу при закрытии контекста.
