# Small tasks

## Connect + ping

* Добавить docker-compose файл и запустить через него сервер БД (MySQL / Postgresql - на ваш вкус)
* Подключиться к БД, используя `database/sql` + подходящий драйвер
* При запуске приложения сделать ping БД


## Create table + insert

* Руками создать таблицу в БД для хранения Notes / Tasks / любой другой сущности
* Создать "репозиторий" и в нем реализовать один метод `Create(ctx context.Context, ...)`


## Query + scan

* Реализовать методы `GetByID(ctx, id)` и `List(ctx, limit, offset)` репозитория
* Убедиться, что в случае `sql.ErrNoRows` клиент вашего API получит 404
